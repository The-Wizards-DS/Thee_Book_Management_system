def display():
    %sql SELECT * FROM books;

    result = %sql SELECT * FROM books;
    print(result);
    
    def main():
        option = int(input("Please select an option (1-5): "))

    switch = {
        1: add_book_record,
        2: edit,
        3: search,
        4: delete_book,
        5: display
    }
    
    func = switch.get(option, lambda: print("Invalid option"))
    func()
  

main()
